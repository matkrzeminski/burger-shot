from django.urls import path


urlpatterns = [
    # path('countries/', CountryAPIView.as_view(), name='countries'),
    # path('states/', StateAPIView.as_view(), name='states'),
    # path("me/", GetUserAPIView.as_view(), name="get_user"),
    # path("me/addresses/", GetUserAddressesAPIView.as_view(), name="user-addresses"),
               ]
